In file, there are three tab delimited columns.

First column -  source ID
Second Column -  Target ID
Third Column -  Weight

The four files correspond to the four networks for which you need to calculate minimum spanning tree.

If two or more choices are possible for a given step, choose one randomly.